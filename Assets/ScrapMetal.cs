using UnityEngine;

public class ScrapMetal : ItemTemplate
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        base.Start();
        setItemName("ScrapMetal");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
